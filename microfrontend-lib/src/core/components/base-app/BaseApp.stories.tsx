import type { Meta, StoryObj } from "@storybook/react";
import { BaseApp } from "./BaseApp";
/* import Layout from '../../../components/layout-test/LayoutTest';

const meta = {
  title: "Example/BaseApp",
  component: BaseApp,
  tags: ["docsPage"],
  argTypes: {
    children: {
      control: { type: 'object' },
    }
  },
} satisfies Meta<typeof BaseApp>;

export default meta;
type Story = StoryObj<typeof meta>;

export const BaseAppPrimary: Story = {
  args: {
    children: <p>hello</p>
  }
}; */